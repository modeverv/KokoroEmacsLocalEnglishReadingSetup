# my-read / org-noter キーバインド検討

## 現在の方針

org-noter の標準キーバインドは変更しない。読書移動は `j` / `k`、現在文の
読み上げは `SPC` に統一し、`i` はorg-noterのノート挿入専用にする。

my-read 固有キーは、従来どおり my-read フレームの左側読書ウィンドウにカーソルが
ある場合だけ有効である。右上の Org ノートや通常の Org バッファには漏れない。

## org-noter 標準キー（本文側）

| キー | org-noter の機能 | my-read との関係 |
|---|---|---|
| `i` | 現在位置へノートを挿入 | my-read側の割当を解除済み。競合なし |
| `C-i` | 質問省略設定を反転してノート挿入 | ターミナル／GUIで `TAB` と同一扱いになる環境では競合し得る |
| `M-i` | precise note を挿入 | 直接の競合なし |
| `C-M-i` | 質問省略設定を反転して precise note を挿入 | 直接の競合なし |
| `q` | org-noter セッション終了 | EPUB の終了・ウィンドウ終了系操作と意味が近く、誤操作に注意 |
| `M-p` / `M-.` / `M-n` | 前／現在／次のページまたは章へ同期 | Meta移動キーを使う設定とは競合し得る |
| `C-M-p` / `C-M-.` / `C-M-n` | 前／現在／次のノートへ同期 | 直接の競合なし |
| `M-T` | ノートウィンドウ位置を変更 | my-read では固定3段レイアウトを崩すため使用非推奨 |
| `C-c C-c` | PDF本文から右上ノートで `org-ctrl-c-ctrl-c` | PDF限定。my-readとの直接競合なし |
| `C-c C-x` | PDF本文から右上ノートへ `C-c C-x …` を転送 | PDF限定。my-readとの直接競合なし |

右上の `org-noter-notes-mode` では `M-p` / `M-.` / `M-n`、
`C-M-p` / `C-M-.` / `C-M-n`、`M-T` が同じ同期操作として有効になる。

## my-read のキー

| キー | 機能 | org-noterとの関係 |
|---|---|---|
| `j` | 次の一文へ移動（Kindleではページ端で次ページ） | 競合なし |
| `k` | 前の一文へ移動（Kindleではページ端で前ページ） | 競合なし |
| `SPC` | 現在の一文を読み上げ | 競合なし |
| `i` | org-noterで現在位置へノートを挿入 | org-noter専用 |
| `s` | 一文ずつ連続読み上げ。もう一度押すと停止 | 競合なし |
| `C-c C-k` | 読み上げ停止 | 競合なし |
| `u` | 語彙ノートへ保存 | 競合なし |
| `l` / `;` | 前／次の単語へ移動し Lookup を追従 | 競合なし |
| `C-c t` | Kindle / EPUB(PDF) / EWW タブ切替 | 競合なし |
| `C-c o` | 現在のPDF／EPUB／Kindleのorg-noterノートを開く | 競合なし |
| `C-c p` | 段落読み上げ | 競合なし |
| `C-c n` | 一文を読み、次へ進む（単発） | 競合なし |
| `C-c k` | 読み上げ停止 | 競合なし |

Kindleではさらに `r`（再接続）、`C-v` / `M-v`（次／前ページ）、矢印・
`C-n` / `C-p`（表示上の上下移動）が有効になる。

PDF Tools／DocViewのPDFでも `C-v` / `M-v` を次／前ページに統一する。
pdf-tools標準の `SPC` スクロールはmy-read内では単発読み上げが優先される。
PDF Toolsで本文をマウス選択すると、その文へ読み上げ位置が自動的に移る。
選択後は `SPC` でその文を読み、`s` でその位置から連続読み上げを開始できる。

### PDF Toolsで永続ハイライト付きノートを作る

1. PDF本文をマウスでドラッグして選択する。
2. 選択を残したまま `i` を押し、org-noterノートを作る。
3. ハイライトをPDFファイルへ永続化するため `C-x C-s` でPDFを保存する。

`org-noter-highlight-selected-text` は常時有効にしている。`C-u i` はその1回だけ
設定を反転するため、ハイライトなしのノートを作りたい場合に使える。

## 現時点の実用上の扱い

- ノートを作るときは org-noter 標準の `i` を使う。Kindleノートには
  `NOTER_PAGE` に加えて `KINDLE_LOCATION` と `KINDLE_FINGERPRINT` が記録される。
- 前の一文へ戻るときは、PDF／EPUB／Kindleのすべてで `k` を使う。
- 現在の一文だけを読み上げるときは `SPC`、連続読み上げは `s` を使う。
- `M-T` はorg-noter標準機能として残すが、my-readの右3段を維持するため使わない。
- Kindle.appは任意位置へ直接ジャンプするAPIを公開していない。このため同じ
  Kindleページ内のノートは同期ジャンプできるが、別ページのノートからは
  `KINDLE_LOCATION` を見てKindle側をその位置まで移動してから同期する。

## 採用済みの競合対応

読書移動を `j` / `k`、単発読み上げを `SPC` に変更した。my-readとKindle固有の
両キーマップから `i` を削除しているため、org-noterセッション中の `i` は常に
`org-noter-insert-note` として解決される。
