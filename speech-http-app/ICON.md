# App icon

Source: `icon.png`. Generated with the built-in imagegen tool on 2026-09-17.
The build script resizes it into a macOS iconset and bundles `SpeechServer.icns`.
Both `CFBundleIconFile` and the native application's icon use this asset.

Generation prompt:

> Create a polished macOS app icon for a standalone text-to-speech LAN server called Reader Speech Server. Square 1024x1024 composition, rounded square deep teal and midnight blue tile centered with generous transparent outer margin. A beautifully simple ivory open book whose center rises into three mint audio waveform bars, subtle dimensional soft highlights, crisp silhouette readable at 32px. No text, no letters, no watermarks, no mockup scene. Actual transparent background outside the rounded-square tile. One icon only.
